# 配置形状示例，需按当前 DSH 版本核对；请勿覆盖现有配置。
- insert:
    - id: figma-canvas-writer
      name: '@deepseek-ai/dsh-mcp-client'
      config:
        serverName: figma-canvas-writer
        transport: stdio
        command: "<Node 绝对路径>"
        args:
          - "<本仓库路径>/bridge/mcp-bridge.js"
